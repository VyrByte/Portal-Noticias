{
  "name": "portal-noticias-backend",
  "version": "1.0.0",
  "description": "Backend JSON Server para Portal de Noticias",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
